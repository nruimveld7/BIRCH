Add/Edit Applied Events
Use these screens to apply new events and modify existing applied events.

The dropdown can be used to select from the existing list of event codes configured via the Team Setup popup.

Custom single-use events can also be created on the spot.

Reminders and date ranges can be adjusted during event creation and modification.

Ensure "Notify Immediately" is selected for instant email notifications to be distributed to all affected employees.